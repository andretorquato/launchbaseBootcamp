{% extends "layout.njk" %}

{% block content %}

    <div class="card">
        <table>
            <thead>
                <tr>
                    <th>Nome Completo</th>
                    <th>Acompanhamento</th>
                    <th>Ação</th>
                </tr>
            </thead>
            <tbody>
                    {% for teacher in teachers %}
                    <tr>
                        <td>
                            <span style="backgroud-image:url({{teacher.avatar_url}})"></span>
                            {{teacher.name}}
                        </td>
                        <td>{{teacher.services}}</td>
                        <td><a href="teachers/{{teacher.id}}">ver</a></td>
                    </>
                    {% endfor %}
                
            </tbody>
        </table>
    </div>
{% endblock content %}